@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<main>
    <div class="header-row">
        <div class="title-area">
            <h1>Add Blog Detailed</h1>
            <div class="context-line">
                <span>Dashbaord &mdash; <strong>Add BlogDetailed</strong></span>
            </div>
        </div>
    </div>

    <form method="post" enctype="multipart/form-data" class="resource-form-container">

        <div class="form-row">

            <input type="hidden" name="ID" value="@Model.ID" />
            <input type="hidden" name="ImagePath" value="@Model.ImagePath" />


            <div class="form-group">
                <label>Url</label>
                <select name="BlogCardID" class="form-select">
                    <option value="">Select URL</option>

                    @foreach (var item in ViewBag.BlogList)
                    {
                        <option value="@item.ID" selected="@(item.ID == Model.BlogCardID)">
                            @item.URL
                        </option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label>Blog Heading</label>
                <input type="text" name="BlogHeading" value="@Model.BlogHeading" placeholder="Blog Heading" />
            </div>

            <div class="form-group">
                <label>Images</label>
                @if (!string.IsNullOrEmpty(Model.ImagePath))
                {
                    <img src="@Model.ImagePath" width="100" class="mb-2" />
                }
                <div class="file-dropzone">
                    <span class="dropzone-text">Drop file here or <strong>browse</strong></span>
                    <input type="file" name="ImageFile" id="resourceFile" onchange="updateFileName(this)" />
                </div>
            </div>

        </div>

        <div class="form-group">
            <label>Keyword</label>
            <input type="text" name="Keyword" value="@Model.Keyword" placeholder="Keyword" required />
        </div>

        <div class="form-group full-width">
            <label>Paragraph</label>
            <textarea id="editor" name="Paragraph">@Model.Paragraph</textarea>
        </div>

        <div class="form-group full-width">
            <label>Author</label>
            <textarea id="editor2" name="Author">@Model.Author</textarea>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-submit">Save Entries</button>
        </div>

    </form>
</main>

<script>
    ClassicEditor
    .create(document.querySelector('#editor'), {
      toolbar: [ 'heading', '|', 'bold', 'italic', 'link', 'bulletedList', 'numberedList', 'blockQuote', 'undo', 'redo' ]
    })
    .catch(error => {
      console.error(error);
    });
</script>

<script>
    ClassicEditor
        .create(document.querySelector('#editor2'))
        .catch(error => console.error(error));
</script>
